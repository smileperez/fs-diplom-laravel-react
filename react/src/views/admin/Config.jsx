import React from "react";
import PageComponent from "../../components/admin/PageComponent";

export default function Config() {
    // TODO:
    // const matrix = [...Array(updatedHall.rows)].map((item, i) => (
    //     <div key={i}>{updatedHall.rows}</div>
    // ));

    // // [...Array(updatedHall.seats)].map((item, j) => (
    // //     <ESeat key={j}></ESeat>
    // //     ))

    return (
        <PageComponent title="Настройка залов">
            <div className="">Настройка залов</div>
        </PageComponent>
    );
}
